apiVersion:     k8s.jabberwocky.se/v1alpha1
kind:           CloudConfig
metadata:
  name:         cluster
spec:
  server:       http://localhost:8888
  insecure:     true
  #credentials:
    #secret:     cloud-config-credentials
  appList:      services
  profile:      [   , uswest ]
  period:       10
