spring:
  application:
    name:  cloud-config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          searchLocations: file:/config/repository, file:/config/repository/{application}